<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.seguros">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".DatosPoliza"></activity>
        <activity android:name=".ContratarSeguro" />
        <activity android:name=".Anadir_clientes" />
        <activity android:name=".Datos_Seguros" />
        <activity android:name=".AnadirSeguros" />
        <activity android:name=".Datos_comercial" />
        <activity android:name=".AnadirComercial" />
        <activity android:name=".Admin" />
        <activity android:name=".Cliente" />
        <activity android:name=".TodosClientes" />
        <activity android:name=".Comercial" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>